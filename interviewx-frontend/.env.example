# URL of the InterviewX backend API.
# Local development (matches the backend's default PORT=5000):
VITE_API_URL=http://localhost:5000

# When you deploy the backend (Render/Railway/etc), change this to its real URL, e.g.:
# VITE_API_URL=https://interviewx-api.onrender.com